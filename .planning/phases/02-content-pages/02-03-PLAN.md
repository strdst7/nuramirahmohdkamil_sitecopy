---
phase: 02-content-pages
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/insights/page.tsx
autonomous: true
requirements:
  - PAGE-03
  - DSGN-04
  - INFR-03

must_haves:
  truths:
    - "Insights page renders a scrollable feed of journal-style article cards"
    - "Article cards use melting-shape border-radius and Surrealist Echoes shadows"
    - "Title section displays 'Ethereal Inquiries' with rotated offset and border-left accent"
    - "Surreal diagram card shows a figcaption with label and description that slides up on hover"
    - "Layout adapts: single column on mobile, asymmetric 7+5 column on desktop"
  artifacts:
    - path: "src/app/insights/page.tsx"
      provides: "Full Insights page with journal article rendering and surreal diagram"
      min_lines: 100
    - path: "src/app/insights/page.tsx"
      contains: "export const metadata: Metadata"
    - path: "src/app/insights/page.tsx"
      contains: "import { articles }"
  key_links:
    - from: "src/app/insights/page.tsx"
      to: "src/data/insights.ts"
      via: "import { articles } from '@/data/insights'"
      pattern: "import.*articles.*from.*@/data/insights"
    - from: "src/app/insights/page.tsx"
      to: "next/image"
      via: "diagram image"
      pattern: "import.*Image.*from.*next/image"
    - from: "src/app/insights/page.tsx"
      to: "@/components/Icon"
      via: "icon rendering for article bullets"
      pattern: "import.*Icon"
---

<objective>
Port the Insights page prototype into a responsive Next.js page with journal-style article cards, surreal diagram placeholders, and asymmetric fluid layout. Content is driven by `src/data/insights.ts` (D-08).

Purpose: Replace the Phase 1 stub (`src/app/insights/page.tsx`) with the full prototype content — academic observation entries styled as melting journal cards with surreal bullet points, pull quotes, and a diagram card with hover-reveal caption. The Newsreader body text and Epilogue headlines must match the prototype's journal aesthetic.

Output: A Server Component at `src/app/insights/page.tsx` rendering the complete Insights page with metadata export, responsive breakpoints, and data-driven article list.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-pages/02-CONTEXT.md
@.planning/codebase/CONVENTIONS.md §Styling Conventions, §Inconsistencies
@.planning/codebase/STRUCTURE.md §Page-Specific Content Summaries → Insights
@insights_ethereal_inquiries/code.html
@surrealist_echoes/DESIGN.md
@tailwind.config.ts
@src/data/insights.ts (created by Plan 02-01 — Article interface and articles array)
@src/app/insights/page.tsx (current stub — will be replaced)
@src/components/Icon.tsx
@src/components/OrganicCard.tsx
@src/components/MeltingShadow.tsx
@src/components/GlassPanel.tsx

<interfaces>
From src/data/insights.ts (created by Plan 02-01):
```
export interface Article {
  id: string;
  title: string;
  date: string;
  excerpt: string;
  body: string;
  tags: string[];
  iconName: string;     // Material Symbols name, e.g. "pest_control"
  diagramAlt: string;
  diagramSrc: string;   // e.g. "/images/insights-diagram.svg"
}
export const articles: Article[];
```

From src/components/Icon.tsx:
```
export function Icon({ name, size?, fill?, weight?, className? })
// Available: pest_control, hourglass_empty, all_inclusive, auto_stories, category, palette, blur_on, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>task 1: build Insights page with journal article cards from data and ambient background</name>
  <files>src/app/insights/page.tsx</files>
  <read_first>
    - src/app/insights/page.tsx (current stub)
    - src/data/insights.ts (Article interface, articles array structure)
    - insights_ethereal_inquiries/code.html (lines 100-165 — ambient background, title section, melting card, isolated text block)
    - src/components/Icon.tsx (available icons)
  </read_first>
  <action>
Replace `src/app/insights/page.tsx` with a full Server Component. Build in two layers: (1) ambient background + title section + article cards, (2) diagram card — diagram card is task 2.

**Metadata (INFR-03):**
```typescript
export const metadata: Metadata = {
  title: "Ethereal Inquiries",
  description: "Extracting the latent space where logic bends and the machine dreams in vibrant, impossible geometries. Journal entries by Nur Amirah Mohd Kamil.",
  openGraph: {
    title: "Ethereal Inquiries — Nur Amirah",
    description: "Extracting the latent space where logic bends and the machine dreams in vibrant, impossible geometries.",
    type: "website",
  },
};
```

**Ambient background:** Two absolute-positioned blur divs (like prototype lines 102-103):
```
<div className="absolute top-0 left-[-10%] w-[50vw] h-[50vw] bg-surface-tint/5 rounded-full blur-[120px] pointer-events-none mix-blend-screen" />
<div className="absolute bottom-1/4 right-[-5%] w-[40vw] h-[60vw] bg-tertiary/5 rounded-[40%_60%_70%_30%] blur-[150px] pointer-events-none" />
```
Wrap in `<div className="relative overflow-hidden">` container.

**Title section (asymmetric, rotated):**
```
<div className="mb-24 ml-4 md:ml-[10vw] max-w-2xl transform -rotate-2 origin-bottom-left">
  <h1 className="font-headline-lg text-headline-lg text-primary mb-6 drop-shadow-[0_5px_15px_rgba(255,198,107,0.2)]">
    Ethereal Inquiries
  </h1>
  <p className="font-body-lg text-body-lg text-tertiary-fixed-dim/80 italic pl-8 border-l border-outline/30 rounded-bl-full">
    Extracting the latent space where logic bends and the machine dreams in vibrant, impossible geometries.
  </p>
</div>
```

**Fluid grid container:**
```
<div className="grid grid-cols-1 md:grid-cols-12 gap-fluid-gap items-start">
  {/* Left column: articles */}
  <div className="md:col-span-7 flex flex-col gap-24 relative">
    ...
  </div>
  {/* Right column: diagram (task 2 placeholder) */}
  <div className="md:col-span-5 flex flex-col gap-16 mt-16 md:mt-40">
    {/* diagram card goes here — add in task 2 */}
  </div>
</div>
```

**Article rendering loop (left column):**
Map over `articles` array. For each article, render a Melting Card `<article>`:

```tsx
{articles.map((article) => (
  <article key={article.id}
    className="bg-surface-container-low/40 backdrop-blur-md rounded-[50%_40%_30%_60%/40%_50%_60%_50%]
               p-10 md:p-16 shadow-[40px_20px_80px_rgba(0,0,0,0.6)]
               border-t border-l border-surface-tint/10
               transform transition-transform hover:scale-[1.02] duration-1000 ease-out
               relative overflow-hidden"
  >
    {/* Decorative blur accent */}
    <div className="absolute -top-10 -right-10 w-40 h-40 bg-error/5 rounded-full blur-2xl" />
    <h2 className="font-headline-md text-headline-md text-on-surface mb-8">
      {article.title}
    </h2>
    <p className="font-body-md text-body-md text-on-surface-variant mb-6 leading-relaxed">
      {article.body}
    </p>
    <ul className="flex flex-col gap-4 mt-8">
      {article.tags.map((tag, i) => (
        <li key={i} className="flex items-start gap-4">
          <Icon
            name={article.iconName as IconName}
            size={20}
            fill={1}
            className="text-tertiary opacity-70 mt-1"
          />
          <span className="font-body-md text-body-md text-on-surface-variant/80">
            {tag}
          </span>
        </li>
      ))}
    </ul>
  </article>
))}
```

**Isolated pull-quote block** (after article loop, left column):
```
<div className="ml-12 md:ml-32 max-w-md">
  <div className="h-24 w-[1px] bg-gradient-to-b from-outline/40 to-transparent mb-6 ml-4 transform -rotate-12" />
  <p className="font-body-lg text-body-lg text-secondary-fixed-dim italic opacity-80">
    "If the parameters could breathe, they would exhale a heavy, amber-scented mist."
  </p>
</div>
```

**Date display:** Add date below each article title: `<time className="font-label-sm text-label-sm text-on-surface-variant/50 block mb-4">{article.date}</time>`

**Accessibility (D-02):**
- All icons have `aria-hidden="true"` (built into `<Icon>`)
- `<time>` element with semantic date (not just text)
- Heading hierarchy: h1 → h2 (article titles)
- Pull quote is decorative — no aria needed
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - Metadata title is "Ethereal Inquiries" (verify: `grep -c '"Ethereal Inquiries"' src/app/insights/page.tsx` returns ≥ 2)
    - Import from data file (verify: `grep -c "from.*@/data/insights" src/app/insights/page.tsx` returns ≥ 1)
    - articles.map() renders article cards (verify: `grep -c "articles.map" src/app/insights/page.tsx` returns ≥ 1)
    - H1 uses text-headline-lg token (verify: `grep -c "text-headline-lg" src/app/insights/page.tsx` returns ≥ 1)
    - Title uses -rotate-2 transform (verify: `grep -c "\-rotate-2" src/app/insights/page.tsx` returns ≥ 1)
    - Ambient blur divs exist (verify: `grep -c "blur-\[120px\]" src/app/insights/page.tsx` returns ≥ 1)
    - Pull quote exists (verify: `grep -c "amber-scented mist" src/app/insights/page.tsx` returns ≥ 1)
    - No data-alt (verify: `grep -c "data-alt" src/app/insights/page.tsx` returns 0)
    - Responsive: md:grid-cols-12, md:col-span-7, md:ml-[10vw] (verify: `grep -c "md:col-span-7" src/app/insights/page.tsx` returns ≥ 1)
  </acceptance_criteria>
  <done>Insights page renders journal articles from data with melting cards, pull quotes, and ambient background — matching prototype aesthetic.</done>
</task>

<task type="auto" tdd="false">
  <name>task 2: add surreal diagram card with figcaption hover reveal and responsive polish</name>
  <files>src/app/insights/page.tsx</files>
  <read_first>
    - src/app/insights/page.tsx (after task 1 — diagram region is empty)
    - insights_ethereal_inquiries/code.html (lines 167-181 — diagram card with figure/figcaption)
    - src/data/insights.ts (articles[0].diagramSrc, articles[0].diagramAlt)
  </read_first>
  <action>
Fill the right column (`md:col-span-5`) with the surreal diagram card. Use the first article's diagram data.

**Diagram card structure:**
```tsx
<figure className="relative w-full rounded-t-[40%] rounded-b-[2rem] overflow-hidden
                     shadow-[20px_40px_80px_rgba(0,0,0,0.8)] border border-outline/10 group">
  <div className="relative w-full h-[500px]">
    <Image
      src={articles[0].diagramSrc}
      alt={articles[0].diagramAlt}
      fill
      className="object-cover opacity-70 group-hover:opacity-100 group-hover:scale-105 transition-all duration-[2000ms] ease-out"
      style={{ mixBlendMode: "luminosity" } as React.CSSProperties}
    />
  </div>
  <figcaption className="absolute bottom-0 w-full bg-surface-container-highest/80 backdrop-blur-xl
                          p-8 pt-12 rounded-t-[3rem] transform translate-y-4
                          group-hover:translate-y-0 transition-transform duration-700">
    <div className="flex items-center gap-4 mb-2">
      <Icon name="all_inclusive" size={20} fill={1} className="text-primary" />
      <span className="font-label-sm text-label-sm text-primary uppercase tracking-widest">
        Fig 1. Latent Topography
      </span>
    </div>
    <p className="font-body-md text-body-md text-on-surface-variant">
      Visualization of gradient descent becoming trapped in a local minimum
      that physically resembles an endless, echoing hallway.
    </p>
  </figcaption>
</figure>
```

**Key prototype details preserved:**
- `rounded-t-[40%]` on top, `rounded-b-[2rem]` on bottom (asymmetric)
- `translate-y-4` → `group-hover:translate-y-0` for caption slide-up on hover
- `opacity-70 group-hover:opacity-100 group-hover:scale-105` for image reveal on hover
- `mix-blend-luminosity` on image (CSS `mixBlendMode` via style prop)
- `duration-[2000ms]` for very slow image transition (prototype quirk worth preserving per D-01)
- `rounded-t-[3rem]` on figcaption (only top corners, bottom square)

**Accessibility (D-02):**
- `alt` text on Image uses the article's diagramAlt field (descriptive: "Surrealist 3D render of an abstract artificial neural network...")
- `<figure>` and `<figcaption>` provide semantic relationship between image and caption
- Icon uses aria-hidden (built into `<Icon>`)

**Responsive behavior:**
- Right column has `mt-16 md:mt-40` — stacks below articles on mobile with less top margin
- Diagram card is full width in single-column layout, constrained to col-span-5 in desktop grid
- `h-[500px]` on image container — tall enough for visual impact but not overwhelming on mobile
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - Figure element exists (verify: `grep -c "<figure" src/app/insights/page.tsx` returns ≥ 1)
    - Figcaption exists with hover slide-up (verify: `grep -c "group-hover:translate-y-0" src/app/insights/page.tsx` returns ≥ 1)
    - Image uses articles[0].diagramSrc (verify: `grep -c "articles\[0\].diagramSrc\|articles\[0\]\.diagramSrc" src/app/insights/page.tsx` returns ≥ 1)
    - Image has real alt text from data (verify: `grep -c "diagramAlt" src/app/insights/page.tsx` returns ≥ 1)
    - Icon "all_inclusive" used (verify: `grep -c 'name="all_inclusive"' src/app/insights/page.tsx` returns ≥ 1)
    - mix-blend-luminosity applied (verify: `grep -c "mixBlendMode" src/app/insights/page.tsx` returns ≥ 1)
    - No Google CDN image URLs (verify: `grep -c "googleusercontent" src/app/insights/page.tsx` returns 0)
    - Responsive: md:col-span-5 for right column (verify: `grep -c "md:col-span-5" src/app/insights/page.tsx` returns ≥ 1)
    - TypeScript compiles cleanly (verify: `npx tsc --noEmit` exits 0)
  </acceptance_criteria>
  <done>Surreal diagram card renders with figcaption hover reveal, responsive layout, and accessible image — matching prototype.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server | Static page — no user input |
| data file → page | articles array is developer-authored constant — trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | page.tsx metadata | accept | OG metadata is intentional for social sharing — no secrets |
| T-02-06 | Tampering | data/insights.ts | accept | Data is hardcoded constants — no external mutation path |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `npx next build` completes without errors
- `npx next dev` serves insights page at localhost:3000/insights
- Title section renders with -2° rotation and border-left accent
- Journal articles render from data with melting card aesthetic, bullet icons
- Diagram card shows image with luminosity blend, caption slides up on hover
- Pull quote block renders with gradient divider line
- Mobile: single column, title at full width with reduced left margin
- Desktop: asymmetric 7+5 grid with offset title
</verification>

<success_criteria>
- Insights page fully replaces Phase 1 stub with prototype-faithful content
- Article cards render from `src/data/insights.ts` with melting-shape border-radius
- Surreal diagram card has hover-reveal figcaption matching prototype behavior
- Ambient background blurs reproduce the ethereal atmosphere
- Layout is responsive: single column mobile, asymmetric 7+5 desktop
- All images have descriptive alt text and use Next.js Image component
- Lighthouse score ≥ 90 on all four metrics
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pages/02-03-SUMMARY.md`
</output>
