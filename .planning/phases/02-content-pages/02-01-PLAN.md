---
phase: 02-content-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/insights.ts
  - src/data/projects.ts
  - public/images/home-hero-bg.svg
  - public/images/insights-diagram.svg
  - public/images/portfolio-island-1.svg
  - public/images/portfolio-island-2.svg
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/components/Footer.tsx
autonomous: true
requirements:
  - INFR-02
  - INFR-03

must_haves:
  truths:
    - "Content data is importable by page components as typed TypeScript constants"
    - "Local image assets exist in /public/images/ and render when referenced"
    - "sitemap.xml and robots.txt are served at their canonical routes"
    - "Footer displays DREAM_VOID brand tagline per D-05"
  artifacts:
    - path: "src/data/insights.ts"
      provides: "Typed article data array for Insights page"
      exports: ["articles"]
    - path: "src/data/projects.ts"
      provides: "Typed project data array for Portfolio page"
      exports: ["projects"]
    - path: "public/images/home-hero-bg.svg"
      provides: "Hero background placeholder replacing Google-hosted image"
    - path: "public/images/insights-diagram.svg"
      provides: "Diagram placeholder for Insights figure"
    - path: "public/images/portfolio-island-1.svg"
      provides: "Project card image for island 1"
    - path: "public/images/portfolio-island-2.svg"
      provides: "Project card image for island 2"
    - path: "src/app/sitemap.ts"
      provides: "Sitemap XML generation"
      exports: ["default"]
    - path: "src/app/robots.ts"
      provides: "Robots.txt generation"
      exports: ["default"]
  key_links:
    - from: "src/app/insights/page.tsx"
      to: "src/data/insights.ts"
      via: "import { articles }"
      pattern: "import.*articles.*from.*@/data/insights"
    - from: "src/app/portfolio/page.tsx"
      to: "src/data/projects.ts"
      via: "import { projects }"
      pattern: "import.*projects.*from.*@/data/projects"
    - from: "src/app/sitemap.ts"
      to: "route /sitemap.xml"
      via: "Next.js file convention"
      pattern: "sitemap\\.ts"
---

<objective>
Set up the data layer, local image assets, and SEO infrastructure for Phase 2 content pages.

Purpose: Pages need typed content data to render articles and projects (D-08), local images to replace Google-hosted CDN placeholders (INFR-02), and discoverable sitemap/robots for search engines (INFR-03). The Footer must also include the DREAM_VOID poetic tagline (D-05).

Output: Two TypeScript data files, four SVG placeholder images, sitemap.ts, robots.ts, and updated Footer component.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-pages/02-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@surrealist_echoes/DESIGN.md
@tailwind.config.ts
@src/components/Footer.tsx
@src/components/Icon.tsx

<interfaces>
<!-- Key types and contracts for downstream executors -->

From src/components/Icon.tsx — icon names available:
```
vibration, unfold_more, category, auto_stories, palette, blur_on,
memory, visibility, science, pest_control, hourglass_empty, water_drop,
all_inclusive, bubble_chart, east, hive, psychology, flare, thermostat,
filter_vintage, blur_circular
```

From tailwind.config.ts — custom tokens available:
- Colors: primary (#ffc66b), on-surface (#ebe2c8), on-surface-variant (#d5c4af), tertiary (#eac9b4), surface-container (#23200f), surface-container-low (#1f1c0b), etc.
- Spacing: meridian (1.5rem), horizon (4rem), fluid-gap (2.5rem), drip (0.75rem)
- Fonts: font-headline-lg (Epilogue 200), font-headline-md (Epilogue 300), font-body-lg (Newsreader 400), font-body-md (Newsreader 400), font-label-sm (Space Grotesk 500)
- Font sizes: text-headline-lg (48px/1.1), text-headline-md (32px/1.2), text-body-lg (20px/1.6), text-body-md (16px/1.6), text-label-sm (12px/1.0)

From next-sitemap convention (Next.js 16, unchanged):
- `src/app/sitemap.ts` exports default function returning MetadataRoute.Sitemap
- `src/app/robots.ts` exports default function returning MetadataRoute.Robots
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>task 1: create content data files with typed article and project data</name>
  <files>src/data/insights.ts, src/data/projects.ts</files>
  <read_first>
    - surrealist_echoes/DESIGN.md (design philosophy for content tone)
    - .planning/phases/02-content-pages/02-CONTEXT.md §Content data strategy (D-08, D-09)
  </read_first>
  <action>
Create `src/data/insights.ts` with a typed Article interface and exported `articles` array (per D-08). Content is hardcoded TypeScript constants.

```typescript
// src/data/insights.ts
export interface Article {
  id: string;
  title: string;          // e.g. "Observation 04.12"
  date: string;           // ISO date string
  excerpt: string;        // 1-2 sentences for feed card
  body: string;           // Full article body text
  tags: string[];         // e.g. ["latent space", "topology"]
  iconName: string;       // Material Symbols icon name from Icon.tsx
  diagramAlt: string;     // Alt text for diagram image
  diagramSrc: string;     // Path to local image in /public/
}

const articles: Article[] = [
  {
    id: "obs-04-12",
    title: "Observation 04.12",
    date: "2025-03-18",
    excerpt: "The weights adjusting in layer 4 evoke a sensation akin to watching wax melt over a rusted gear.",
    body: "The weights adjusting in layer 4 evoke a sensation akin to watching wax melt over a rusted gear. It is not learning; it is an erosion of prior states. We queried the model on the concept of 'time', and it returned a series of topological maps where clocks physically drooped over landscape features.",
    tags: ["Latent Space", "Topology"],
    iconName: "pest_control",
    diagramAlt: "Surrealist 3D render of an abstract artificial neural network forming melting, organic shapes in a dimly lit barren room, warm amber lighting",
    diagramSrc: "/images/insights-diagram.svg",
  },
];
```

Create `src/data/projects.ts` with Project interface and exported `projects` array. Content mirrors prototype cards.

```typescript
// src/data/projects.ts
export interface Project {
  id: string;
  number: string;          // e.g. "Project 01"
  title: string;           // e.g. "Echoes of the Melted Grid"
  description: string;     // 2-3 sentence description
  tags: string[];          // e.g. ["UX Subversion", "Tactile"]
  iconName: string;        // Material Symbols icon from Icon.tsx
  imageAlt: string;        // Real alt text for accessibility (D-02)
  imageSrc: string;        // Path to local image in /public/
  cta?: { label: string; href: string };  // optional CTA button
  layout: "right" | "left"; // which side the card drifts toward
}

const projects: Project[] = [
  {
    id: "echoes-melted-grid",
    number: "Project 01",
    title: "Echoes of the Melted Grid",
    description: "A study in structural decay. What happens when the rigid constraints of modernism are exposed to the heat of irrationality?",
    tags: ["UX Subversion", "Tactile"],
    iconName: "hive",
    imageAlt: "Abstract digital artwork showing flowing, melting liquid forms in amber and deep blue colors with soft volumetric lighting",
    imageSrc: "/images/portfolio-island-1.svg",
    layout: "right",
  },
  {
    id: "weightless-atelier",
    number: "Project 02",
    title: "The Weightless Atelier",
    description: "Designing for environments without gravity. UI components that behave like liquids in zero-G, responding to proximity rather than direct touch.",
    tags: ["Zero-G UI", "Proximity"],
    iconName: "bubble_chart",
    imageAlt: "Surreal minimalist composition of floating smooth spheres and curved objects over a dark infinite horizon with ethereal dramatic lighting",
    imageSrc: "/images/portfolio-island-2.svg",
    cta: { label: "Explore Fluidity", href: "#" },
    layout: "left",
  },
];
```

Both files use default exports for the arrays plus named exports for the interfaces. Place in `src/data/` (create directory if missing).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/insights.ts` exists and exports `articles` (verify: `grep -v '^#' src/data/insights.ts | grep -c "export const articles"` returns ≥ 1)
    - `src/data/projects.ts` exists and exports `projects` (verify: `grep -v '^#' src/data/projects.ts | grep -c "export const projects"` returns ≥ 1)
    - Both files pass TypeScript compilation (verify: `npx tsc --noEmit` exits 0)
    - Each file exports its interface type (verify: `grep -v '^#' src/data/insights.ts | grep -c "export interface Article"` returns ≥ 1)
  </acceptance_criteria>
  <done>Content data files created with typed data accessible by downstream page plans.</done>
</task>

<task type="auto" tdd="false">
  <name>task 2: create local SVG placeholder images replacing Google CDN assets</name>
  <files>public/images/home-hero-bg.svg, public/images/insights-diagram.svg, public/images/portfolio-island-1.svg, public/images/portfolio-island-2.svg</files>
  <read_first>
    - home_the_persistence_of_intelligence/code.html (lines 122-123 for hero bg context)
    - insights_ethereal_inquiries/code.html (lines 169-171 for diagram context)
    - portfolio_archive_of_dreams/code.html (lines 160, 194 for island image context)
    - .planning/phases/02-content-pages/02-CONTEXT.md §Prototype source
  </read_first>
  <action>
Create 4 SVG placeholder images in `/public/images/` to replace Google-hosted AIDA CDN images (INFR-02). Each SVG uses the Surrealist Echoes palette and conveys the visual intent described in prototype `data-alt` attributes.

Create directory first: `mkdir -p public/images`

**home-hero-bg.svg**: Abstract desert landscape with warm amber/gold tones. Uses `radialGradient` from `#e8a838` (primary-container) to `#171305` (background). Soft undulating shapes suggesting dunes. 800×600 viewBox. Includes subtle blur simulation via opacity layers.

**insights-diagram.svg**: Surrealist 3D render of a neural network as melting organic forms. Warm amber lighting against a dim barren room. Uses `#ffc66b` (primary) accents on `#171305` (bg). Abstract connected nodes with variable-radius blob containers. 600×500 viewBox.

**portfolio-island-1.svg**: Flowing, melting abstract liquid forms in amber (`#ffc66b`) and deep blue (`#a4c9ff`) with soft volumetric lighting. Uses gradient meshes and curved path elements. 800×600 viewBox.

**portfolio-island-2.svg**: Floating smooth spheres and curved objects over a dark infinite horizon. Ethereal dramatic lighting with `#eac9b4` (tertiary) gradients. Negative space conveys weightlessness. 600×800 viewBox.

All SVGs: embed directly (no external references), use viewBox, include `xmlns="http://www.w3.org/2000/svg"`.
  </action>
  <verify>
    <automated>ls public/images/home-hero-bg.svg public/images/insights-diagram.svg public/images/portfolio-island-1.svg public/images/portfolio-island-2.svg 2>&amp;1</automated>
  </verify>
  <acceptance_criteria>
    - All 4 SVG files exist in public/images/ (verify: `ls public/images/*.svg | wc -l` returns 4)
    - Each file is valid SVG (verify: `head -1 public/images/home-hero-bg.svg` contains `xmlns="http://www.w3.org/2000/svg"`)
    - No external references in SVGs (verify: `grep -l "http" public/images/*.svg` returns empty for URLs — only xmlns is allowed)
    - Each SVG uses Surrealist Echoes color values: `#ffc66b`, `#e8a838`, `#a4c9ff`, `#eac9b4`, `#171305` (verify: `grep -c '#ffc66b\|#e8a838\|#a4c9ff\|#eac9b4\|#171305' public/images/*.svg` returns ≥ 8 total matches across files)
  </acceptance_criteria>
  <done>Local SVG placeholders exist in /public/images/, ready for Next.js <Image> component usage.</done>
</task>

<task type="auto" tdd="false">
  <name>task 3: create sitemap, robots, and update Footer with DREAM_VOID tagline</name>
  <files>src/app/sitemap.ts, src/app/robots.ts, src/components/Footer.tsx</files>
  <read_first>
    - src/components/Footer.tsx (current state to modify)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/01-metadata/sitemap.md
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/01-metadata/robots.md
    - .planning/phases/02-content-pages/02-CONTEXT.md §Brand identity (D-05)
  </read_first>
  <action>
**1. Create `src/app/sitemap.ts`:**
```typescript
import type { MetadataRoute } from "next";

export default function sitemap(): MetadataRoute.Sitemap {
  const baseUrl = process.env.NEXT_PUBLIC_SITE_URL || "https://nuramirah.com";
  return [
    { url: baseUrl, lastModified: new Date(), changeFrequency: "yearly", priority: 1 },
    { url: `${baseUrl}/insights`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.8 },
    { url: `${baseUrl}/portfolio`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.8 },
    { url: `${baseUrl}/playground`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.7 },
  ];
}
```

**2. Create `src/app/robots.ts`:**
```typescript
import type { MetadataRoute } from "next";

export default function robots(): MetadataRoute.Robots {
  const baseUrl = process.env.NEXT_PUBLIC_SITE_URL || "https://nuramirah.com";
  return {
    rules: { userAgent: "*", allow: "/" },
    sitemap: `${baseUrl}/sitemap.xml`,
  };
}
```

**3. Update `src/components/Footer.tsx` per D-05:**
Add DREAM_VOID poetic tagline above the copyright line. Use the `font-label-sm text-label-sm` token, amber-500 color, with `drop-shadow-[0_5px_15px_rgba(232,168,56,0.2)]` for the prototype glow effect. The updated footer JSX structure:

```tsx
<footer className="py-horizon px-horizon text-center bg-gradient-to-t from-amber-950/10 to-transparent">
  {/* DREAM_VOID poetic tagline per D-05 */}
  <p className="font-label-sm text-label-sm text-amber-500 tracking-widest drop-shadow-[0_5px_15px_rgba(232,168,56,0.2)] mb-drip">
    DREAM_VOID
  </p>
  <p className="font-body-md text-body-md text-on-surface-variant/60">
    &copy; {new Date().getFullYear()} Nur Amirah Mohd Kamil
  </p>
  <div className="flex justify-center gap-meridian mt-drip">
    <a
      href="https://linkedin.com/in/nuramirahmohdkamil"
      target="_blank"
      rel="noopener noreferrer"
      className="font-label-sm text-label-sm text-on-surface-variant/50 hover:text-primary transition-colors uppercase"
    >
      LinkedIn
    </a>
    <a
      href="mailto:hello@nuramirah.com"
      className="font-label-sm text-label-sm text-on-surface-variant/50 hover:text-primary transition-colors uppercase"
    >
      Email
    </a>
  </div>
</footer>
```

Key changes from current Footer.tsx:
- Added `bg-gradient-to-t from-amber-950/10 to-transparent` to footer container
- Added DREAM_VOID line `<p className="font-label-sm text-label-sm text-amber-500 tracking-widest drop-shadow-[0_5px_15px_rgba(232,168,56,0.2)] mb-drip">DREAM_VOID</p>`
- Preserve existing LinkedIn and Email links, copyright line
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/sitemap.ts` exists and exports default function (verify: `grep -v '^#' src/app/sitemap.ts | grep -c "export default function sitemap"` returns ≥ 1)
    - `src/app/robots.ts` exists and exports default function (verify: `grep -v '^#' src/app/robots.ts | grep -c "export default function robots"` returns ≥ 1)
    - Footer contains DREAM_VOID tagline (verify: `grep -c "DREAM_VOID" src/components/Footer.tsx` returns ≥ 1)
    - Footer has gradient background (verify: `grep -c "bg-gradient-to-t" src/components/Footer.tsx` returns ≥ 1)
    - No TypeScript errors (verify: `npx tsc --noEmit` exits 0)
  </acceptance_criteria>
  <done>Sitemap and robots files created. Footer displays DREAM_VOID brand tagline per D-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → app | Data files are developer-authored constants — trusted source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | sitemap.ts | accept | Site is public portfolio — sitemap exposure is intentional and desirable |
| T-02-02 | Information Disclosure | robots.ts | accept | Allow-all robots.txt is correct for public portfolio — no private routes exist |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `ls public/images/*.svg | wc -l` returns exactly 4
- Both data files export typed arrays
- Footer component renders DREAM_VOID tagline
- Sitemap and robots routes resolve: `curl http://localhost:3000/sitemap.xml` returns XML, `curl http://localhost:3000/robots.txt` returns text
</verification>

<success_criteria>
- All 4 SVG placeholder images exist and are valid
- TypeScript compilation passes with zero errors
- Content data is importable with full type safety
- Sitemap XML and robots.txt are discoverable at canonical routes
- Footer includes DREAM_VOID tagline matching prototype aesthetic
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pages/02-01-SUMMARY.md`
</output>
